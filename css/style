/*
 * HaitiTPS.us — Static Site Edition
 * Sagesse Solutions Administratives (SSA)
 * Version: 2.1.0
 */

/* === RESET & VARIABLES === */
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
:root{
  --ht-blue:#00209F;
  --ht-red:#D21034;
  --ht-gold:#F1C40F;
  --ht-dark:#1a1a2e;
  --ht-navy:#16213e;
  --ht-white:#ffffff;
  --ht-light:#f0f2f5;
  --ht-gray:#6c757d;
  --ht-light-gray:#e9ecef;
  --ht-success:#28a745;
  --ht-accent:#0f3460;
  --ht-gradient:linear-gradient(135deg,var(--ht-blue) 0%,var(--ht-navy) 100%);
  --ht-gradient-warm:linear-gradient(135deg,var(--ht-red) 0%,#a00d28 100%);
  --ht-shadow:0 4px 20px rgba(0,0,0,.1);
  --ht-shadow-lg:0 10px 40px rgba(0,0,0,.15);
  --ht-radius:12px;
  --ht-radius-sm:8px;
  --ht-transition:all .3s ease;
  --ht-font-primary:'Poppins','Segoe UI',sans-serif;
  --ht-font-heading:'Montserrat','Segoe UI',sans-serif;
}
html{scroll-behavior:smooth;font-size:16px}
body{font-family:var(--ht-font-primary);color:var(--ht-dark);background:var(--ht-white);line-height:1.7;overflow-x:hidden}
a{color:var(--ht-blue);text-decoration:none;transition:var(--ht-transition)}
a:hover{color:var(--ht-red)}
img{max-width:100%;height:auto}
.container{max-width:1200px;margin:0 auto;padding:0 20px}

/* === LANGUAGE TOGGLE === */
.lang-toggle-wrapper{position:fixed;top:20px;right:20px;z-index:9999;display:flex;flex-direction:column;gap:6px}
.lang-btn{display:flex;align-items:center;justify-content:center;width:48px;height:48px;border-radius:50%;border:3px solid var(--ht-white);background:var(--ht-gradient);color:var(--ht-white);font-weight:700;font-size:13px;cursor:pointer;transition:var(--ht-transition);box-shadow:var(--ht-shadow);font-family:var(--ht-font-primary)}
.lang-btn:hover{transform:scale(1.1)}
.lang-btn.active{background:var(--ht-red);border-color:var(--ht-gold);transform:scale(1.1)}

/* === WHATSAPP FLOAT === */
.whatsapp-float{position:fixed;bottom:25px;right:25px;width:60px;height:60px;background:#25D366;border-radius:50%;display:flex;align-items:center;justify-content:center;box-shadow:var(--ht-shadow-lg);z-index:9998;transition:var(--ht-transition)}
.whatsapp-float:hover{transform:scale(1.1);background:#1ebe57}
.whatsapp-float svg{width:32px;height:32px;fill:var(--ht-white)}

/* === LEGAL BAR === */
.legal-bar{background:var(--ht-dark);color:rgba(255,255,255,.85);padding:10px 0;font-size:.8rem;border-bottom:3px solid var(--ht-gold);line-height:1.5}
.legal-bar a{color:var(--ht-gold)}
.legal-bar a:hover{color:var(--ht-white);text-decoration:underline}
.legal-bar strong{color:var(--ht-gold)}

/* === HEADER === */
.site-header{background:var(--ht-white);box-shadow:var(--ht-shadow);position:sticky;top:0;z-index:1000}
.header-top{background:var(--ht-dark);color:rgba(255,255,255,.8);padding:6px 0;font-size:.82rem}
.header-top a{color:var(--ht-gold)}
.header-top a:hover{color:var(--ht-white)}
.header-main{padding:12px 0}
.header-main .container{display:flex;align-items:center;justify-content:space-between}
.site-brand{display:flex;align-items:center;gap:12px}
.site-brand h1{font-family:var(--ht-font-heading);font-size:1.5rem;font-weight:800;color:var(--ht-blue)}
.site-brand h1 span{color:var(--ht-gold)}
.site-brand small{display:block;font-size:.7rem;color:var(--ht-gray);font-weight:400}
.main-nav ul{list-style:none;display:flex;gap:6px}
.main-nav a{padding:8px 14px;border-radius:var(--ht-radius-sm);font-weight:500;font-size:.9rem;color:var(--ht-dark);transition:var(--ht-transition)}
.main-nav a:hover,.main-nav a.active{background:var(--ht-blue);color:var(--ht-white)}
.menu-toggle{display:none;background:none;border:2px solid var(--ht-blue);color:var(--ht-blue);font-size:1.5rem;padding:4px 10px;border-radius:var(--ht-radius-sm);cursor:pointer}

/* === HERO SECTION === */
.hero-section{background:var(--ht-gradient);color:var(--ht-white);padding:80px 0 60px;position:relative;overflow:hidden}
.hero-section::before{content:'';position:absolute;top:-50%;right:-10%;width:500px;height:500px;background:radial-gradient(circle,rgba(241,196,15,.15) 0%,transparent 70%);border-radius:50%}
.hero-badge{display:inline-block;background:rgba(255,255,255,.15);padding:8px 20px;border-radius:50px;font-size:.85rem;font-weight:600;margin-bottom:20px;backdrop-filter:blur(10px)}
.hero-title{font-family:var(--ht-font-heading);font-size:2.5rem;font-weight:800;line-height:1.2;margin-bottom:20px}
.hero-title .highlight{color:var(--ht-gold)}
.hero-subtitle{font-size:1.1rem;opacity:.9;margin-bottom:30px;max-width:700px}
.hero-buttons{display:flex;flex-wrap:wrap;gap:12px;margin-bottom:40px}
.hero-stats{display:flex;gap:40px;padding-top:30px;border-top:1px solid rgba(255,255,255,.2)}
.stat-item{text-align:center}
.stat-number{font-family:var(--ht-font-heading);font-size:2rem;font-weight:800}
.stat-label{font-size:.85rem;opacity:.8}

/* === BUTTONS === */
.btn{display:inline-flex;align-items:center;padding:12px 28px;border-radius:50px;font-weight:600;font-size:.95rem;transition:var(--ht-transition);cursor:pointer;border:none;text-align:center}
.btn-primary{background:var(--ht-gold);color:var(--ht-dark)}
.btn-primary:hover{background:#e0b200;color:var(--ht-dark);transform:translateY(-2px);box-shadow:var(--ht-shadow)}
.btn-secondary{background:rgba(255,255,255,.15);color:var(--ht-white);border:2px solid var(--ht-white)}
.btn-secondary:hover{background:var(--ht-white);color:var(--ht-blue)}
.btn-red{background:var(--ht-red);color:var(--ht-white)}
.btn-red:hover{background:#a00d28;transform:translateY(-2px)}
.btn-blue{background:var(--ht-blue);color:var(--ht-white)}
.btn-blue:hover{background:var(--ht-navy);transform:translateY(-2px)}

/* === SECTIONS === */
.section{padding:60px 0}
.section-header{text-align:center;margin-bottom:40px}
.section-header h2{font-family:var(--ht-font-heading);font-size:2rem;font-weight:700;color:var(--ht-dark)}
.section-header p{color:var(--ht-gray);margin-top:10px;max-width:600px;margin-left:auto;margin-right:auto}
.underline{width:60px;height:4px;background:var(--ht-gold);margin:0 auto 15px;border-radius:2px}

/* === TPS ALERT BOX === */
.tps-alert{background:linear-gradient(135deg,#fff9e6 0%,#fff3cc 100%);border-left:5px solid var(--ht-gold);padding:25px 30px;border-radius:var(--ht-radius);margin-bottom:30px;box-shadow:var(--ht-shadow)}
.tps-alert h3{color:var(--ht-dark);font-family:var(--ht-font-heading);margin-bottom:12px;font-size:1.2rem}
.tps-alert p{margin-bottom:10px;font-size:.95rem}
.tps-alert .alert-date{font-size:.8rem;color:var(--ht-gray);font-style:italic;margin-top:15px}

/* === SERVICE CARDS === */
.services-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:24px;margin-top:30px}
.service-card{background:var(--ht-white);border-radius:var(--ht-radius);padding:28px;box-shadow:var(--ht-shadow);transition:var(--ht-transition);border:1px solid var(--ht-light-gray);position:relative;overflow:hidden}
.service-card:hover{transform:translateY(-5px);box-shadow:var(--ht-shadow-lg)}
.service-card::before{content:'';position:absolute;top:0;left:0;width:100%;height:4px;background:var(--ht-gradient)}
.service-card h3{font-family:var(--ht-font-heading);font-size:1.1rem;margin:12px 0 8px;color:var(--ht-dark)}
.service-card p{font-size:.9rem;color:var(--ht-gray)}
.form-label{display:inline-block;background:rgba(0,32,159,.1);color:var(--ht-blue);padding:4px 12px;border-radius:50px;font-size:.75rem;font-weight:600}

/* === DETAIL BLOCKS (Services page) === */
.detail-block{background:var(--ht-white);border-left:5px solid var(--ht-blue);padding:25px 30px;border-radius:var(--ht-radius);margin-bottom:24px;box-shadow:var(--ht-shadow)}
.detail-block h3{color:var(--ht-blue);font-family:var(--ht-font-heading);margin-bottom:12px;font-size:1.15rem}
.detail-block p{margin-bottom:10px;font-size:.95rem}

/* === TRIVIA GAME === */
.trivia-section{background:var(--ht-light);padding:60px 0}
.trivia-container{max-width:700px;margin:0 auto}
.trivia-card{background:var(--ht-white);border-radius:var(--ht-radius);padding:30px;box-shadow:var(--ht-shadow-lg);text-align:center}
.trivia-progress{font-size:.85rem;color:var(--ht-gray);margin-bottom:15px}
.trivia-question{font-family:var(--ht-font-heading);font-size:1.2rem;font-weight:600;margin-bottom:25px;color:var(--ht-dark)}
.trivia-options{display:grid;gap:12px;margin-bottom:20px}
.trivia-option{display:block;width:100%;padding:14px 20px;border:2px solid var(--ht-light-gray);border-radius:var(--ht-radius-sm);background:var(--ht-white);font-size:.95rem;cursor:pointer;transition:var(--ht-transition);text-align:left;font-family:var(--ht-font-primary);color:var(--ht-dark)}
.trivia-option:hover{border-color:var(--ht-blue);background:rgba(0,32,159,.05)}
.trivia-option.correct{border-color:var(--ht-success);background:rgba(40,167,69,.1);color:var(--ht-success);font-weight:600}
.trivia-option.wrong{border-color:var(--ht-red);background:rgba(210,16,52,.1);color:var(--ht-red)}
.trivia-option:disabled{cursor:not-allowed;opacity:.7}
.trivia-explanation{background:rgba(0,32,159,.05);border-radius:var(--ht-radius-sm);padding:15px;margin:15px 0;text-align:left;font-size:.9rem;display:none;border-left:4px solid var(--ht-blue)}
.trivia-explanation.show{display:block}
.trivia-next{display:none;margin-top:15px}
.trivia-next.show{display:inline-flex}
.trivia-score{text-align:center;padding:30px}
.trivia-score h3{font-family:var(--ht-font-heading);font-size:1.5rem;margin-bottom:10px}
.trivia-score p{margin-bottom:15px;color:var(--ht-gray)}

/* === FAQ ACCORDION === */
.faq-item{background:var(--ht-white);border-radius:var(--ht-radius-sm);margin-bottom:10px;box-shadow:0 2px 8px rgba(0,0,0,.06);overflow:hidden;border:1px solid var(--ht-light-gray)}
.faq-question{padding:18px 24px;font-weight:600;cursor:pointer;display:flex;justify-content:space-between;align-items:center;font-size:.95rem;transition:var(--ht-transition)}
.faq-question:hover{background:rgba(0,32,159,.03)}
.faq-question::after{content:'+';font-size:1.3rem;font-weight:700;color:var(--ht-blue);transition:var(--ht-transition)}
.faq-item.open .faq-question::after{content:'\2212';color:var(--ht-red)}
.faq-answer{max-height:0;overflow:hidden;transition:max-height .3s ease;padding:0 24px}
.faq-item.open .faq-answer{max-height:500px;padding:0 24px 18px}

/* === ABOUT HERO / PAGE HERO === */
.about-hero{background:var(--ht-gradient);color:var(--ht-white);padding:60px 0 40px;text-align:center}
.about-hero h1{font-family:var(--ht-font-heading);font-size:2.2rem;font-weight:800;margin-bottom:12px}
.about-hero p{opacity:.9;max-width:600px;margin:0 auto;font-size:1.05rem}

/* === ABOUT CONTENT === */
.about-grid{display:grid;grid-template-columns:1fr 1fr;gap:40px;padding:40px 0}
.about-text h2{font-family:var(--ht-font-heading);font-size:1.5rem;margin-bottom:15px;color:var(--ht-blue)}
.about-text p{margin-bottom:15px}
.values-list{list-style:none;padding:0}
.values-list li{padding:10px 0;border-bottom:1px solid var(--ht-light-gray);display:flex;align-items:flex-start;gap:10px}
.values-list .check{color:var(--ht-success);font-weight:700;font-size:1.1rem}
.do-dont{display:grid;grid-template-columns:1fr 1fr;gap:20px;margin-top:30px}
.do-col,.dont-col{padding:25px;border-radius:var(--ht-radius);font-size:.9rem}
.do-col{background:rgba(40,167,69,.08);border:1px solid rgba(40,167,69,.2)}
.do-col h3{color:var(--ht-success)}
.dont-col{background:rgba(210,16,52,.05);border:1px solid rgba(210,16,52,.15)}
.dont-col h3{color:var(--ht-red)}
.do-col ul,.dont-col ul{list-style:none;margin-top:10px}
.do-col li,.dont-col li{padding:5px 0;padding-left:18px;position:relative}
.do-col li::before{content:'\2713';position:absolute;left:0;color:var(--ht-success);font-weight:700}
.dont-col li::before{content:'\2717';position:absolute;left:0;color:var(--ht-red);font-weight:700}

/* === CONTACT === */
.contact-section{padding:60px 0}
.contact-grid{display:grid;grid-template-columns:1fr 1.2fr;gap:30px}
.contact-info{background:var(--ht-gradient);color:var(--ht-white);padding:35px;border-radius:var(--ht-radius);box-shadow:var(--ht-shadow-lg)}
.contact-info h3{font-family:var(--ht-font-heading);margin-bottom:25px;font-size:1.3rem}
.contact-item{display:flex;gap:15px;margin-bottom:20px;align-items:flex-start}
.contact-item-icon{font-size:1.5rem;min-width:35px;text-align:center}
.contact-item-text h4{font-size:.95rem;margin-bottom:4px}
.contact-item-text p{font-size:.9rem;opacity:.9}
.contact-form-wrap{background:var(--ht-white);padding:35px;border-radius:var(--ht-radius);box-shadow:var(--ht-shadow-lg);border:1px solid var(--ht-light-gray)}
.form-group{margin-bottom:18px}
.form-group label{display:block;font-weight:600;font-size:.9rem;margin-bottom:6px;color:var(--ht-dark)}
.form-group input,.form-group select,.form-group textarea{width:100%;padding:12px 16px;border:2px solid var(--ht-light-gray);border-radius:var(--ht-radius-sm);font-size:.95rem;font-family:var(--ht-font-primary);transition:var(--ht-transition);background:var(--ht-white);color:var(--ht-dark)}
.form-group input:focus,.form-group select:focus,.form-group textarea:focus{outline:none;border-color:var(--ht-blue);box-shadow:0 0 0 3px rgba(0,32,159,.1)}
.form-group textarea{resize:vertical;min-height:100px}
.consent-group{display:flex;align-items:flex-start;gap:10px;margin-bottom:18px;font-size:.85rem}
.consent-group input[type="checkbox"]{margin-top:3px;accent-color:var(--ht-blue);width:18px;height:18px;cursor:pointer}
.form-success{display:none;background:rgba(40,167,69,.1);border:1px solid var(--ht-success);color:var(--ht-success);padding:15px;border-radius:var(--ht-radius-sm);text-align:center;font-weight:600;margin-top:10px}

/* === CTA SECTION === */
.cta-section{background:var(--ht-gradient-warm);color:var(--ht-white);padding:60px 0;text-align:center}
.cta-section h2{font-family:var(--ht-font-heading);font-size:2rem;font-weight:800;margin-bottom:15px}
.cta-section p{opacity:.9;margin-bottom:25px;max-width:600px;margin-left:auto;margin-right:auto}
.cta-buttons{display:flex;flex-wrap:wrap;gap:15px;justify-content:center}

/* === LEGAL PAGES === */
.legal-page{padding:40px 0 60px}
.legal-page .container{max-width:850px}
.legal-page h2{font-family:var(--ht-font-heading);font-size:1.3rem;color:var(--ht-blue);margin:30px 0 12px;padding-top:20px;border-top:1px solid var(--ht-light-gray)}
.legal-page h2:first-of-type{border-top:none;margin-top:20px}
.legal-page p{margin-bottom:12px;font-size:.95rem}
.legal-page ul{margin:10px 0 15px 25px;font-size:.93rem}
.legal-page li{margin-bottom:6px}
.last-updated{font-size:.85rem;color:var(--ht-gray);font-style:italic;margin-bottom:20px}

/* === FOOTER === */
.site-footer{background:var(--ht-dark);color:rgba(255,255,255,.8);padding:50px 0 0}
.footer-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:30px}
.footer-col h4{color:var(--ht-white);font-family:var(--ht-font-heading);margin-bottom:15px;font-size:1rem}
.footer-col p{font-size:.88rem;line-height:1.6}
.footer-col ul{list-style:none}
.footer-col li{margin-bottom:8px}
.footer-col a{color:rgba(255,255,255,.7);font-size:.88rem}
.footer-col a:hover{color:var(--ht-gold)}
.footer-bottom{margin-top:40px;padding:20px 0;border-top:1px solid rgba(255,255,255,.1);text-align:center;font-size:.82rem;color:rgba(255,255,255,.5)}
.footer-disclaimer{margin-top:20px;padding:15px;background:rgba(241,196,15,.08);border:1px solid rgba(241,196,15,.2);border-radius:var(--ht-radius-sm);font-size:.8rem;line-height:1.5}
.footer-disclaimer strong{color:var(--ht-gold)}

/* === RESPONSIVE === */
@media(max-width:992px){
  .menu-toggle{display:block}
  .main-nav{position:absolute;top:100%;left:0;width:100%;background:var(--ht-white);box-shadow:var(--ht-shadow);display:none;padding:10px 0}
  .main-nav.active{display:block}
  .main-nav ul{flex-direction:column;padding:0 20px}
  .main-nav a{display:block;padding:12px 14px}
  .hero-title{font-size:2rem}
  .hero-stats{flex-wrap:wrap;gap:20px}
  .about-grid{grid-template-columns:1fr}
  .contact-grid{grid-template-columns:1fr}
  .footer-grid{grid-template-columns:1fr 1fr}
  .do-dont{grid-template-columns:1fr}
}
@media(max-width:768px){
  .hero-section{padding:50px 0 40px}
  .hero-title{font-size:1.7rem}
  .hero-buttons{flex-direction:column}
  .btn{width:100%;justify-content:center}
  .services-grid{grid-template-columns:1fr}
  .lang-toggle-wrapper{top:10px;right:10px}
  .lang-btn{width:40px;height:40px;font-size:11px}
  .section{padding:40px 0}
}
@media(max-width:480px){
  .container{padding:0 15px}
  .hero-title{font-size:1.4rem}
  .footer-grid{grid-template-columns:1fr}
  .about-hero h1{font-size:1.6rem}
  .section-header h2{font-size:1.5rem}
}
